#!/bin/bash


curl -s http://localhost:3000/signup \
        --data  '{"name": "Dangelo", "lastname":"Guanipa", "username":"admin",
        "email":"admin@telecentro.net.ar", "password":"admin", "role":"admin"}' \
        -H 'Content-Type: application/json' \
        -X POST | jq -R '. as $raw | try fromjson catch $raw'
